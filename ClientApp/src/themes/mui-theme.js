// External components
import {createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette: {
      type: "light",
      background: {
        default: "#f5f7f8"
      },
      divider: '#e6e6e6',
    },
    typography: {
      fontFamily: "Lato",
    },
    components: {
      
    }
  })

  export default theme